# 等级和道具系统服务器接入完整指南

## 概述

本指南详细说明如何将**等级系统**和**道具系统**接入服务器，实现数据的云端存储和同步。

## 系统架构

### 当前架构

1. **道具系统**：已使用 `ItemDataAdapter` 适配器模式，支持本地/服务器/混合模式
2. **等级系统**：目前使用 `CharacterDataManager`，仅支持本地存储

### 接入后的架构

两个系统都使用适配器模式，可以独立切换存储模式：
- **本地模式**：使用 localStorage（开发/测试）
- **服务器模式**：使用 HTTP API（生产环境）
- **混合模式**：本地缓存 + 服务器同步（推荐，兼顾性能和可靠性）

---

## 第一部分：道具系统服务器接入

### 1.1 当前状态

道具系统已经支持服务器接入，使用 `ItemDataAdapter` 适配器。

### 1.2 快速切换

```javascript
const ItemDataAdapter = require("ItemDataAdapter");

// 设置服务器API地址
ItemDataAdapter.serverConfig.baseURL = "https://your-api-server.com/api";

// 切换到服务器模式
ItemDataAdapter.setStorageMode("server");

// 或使用混合模式（推荐）
ItemDataAdapter.setStorageMode("hybrid");
```

### 1.3 服务器API规范

#### 获取全局道具列表

**请求：**
```
GET /api/inventory
Headers:
  Authorization: Bearer {token}  // 可选，如果需要身份验证
```

**响应：**
```json
{
  "success": true,
  "data": {
    "items": [
      { "itemId": "upgrade_potion", "count": 3 },
      { "itemId": "exp_potion", "count": 5 }
    ]
  }
}
```

#### 保存全局道具列表

**请求：**
```
PUT /api/inventory
Content-Type: application/json
Headers:
  Authorization: Bearer {token}  // 可选

Body:
{
  "items": [
    { "itemId": "upgrade_potion", "count": 3 },
    { "itemId": "exp_potion", "count": 5 }
  ]
}
```

**响应：**
```json
{
  "success": true,
  "message": "保存成功"
}
```

---

## 第二部分：等级系统服务器接入

### 2.1 创建等级数据适配器

首先，我们需要为等级系统创建一个类似 `ItemDataAdapter` 的适配器。

#### 创建 `CharacterDataAdapter.js`

```javascript
/**
 * 角色数据适配器
 * 抽象数据存储层，支持本地存储和服务器存储的切换
 * 
 * 使用适配器模式，可以轻松切换数据源：
 * - 本地模式：使用 localStorage
 * - 服务器模式：使用 HTTP API
 * - 混合模式：本地缓存 + 服务器同步
 */
var CharacterDataAdapter = {
    // 存储模式：'local' | 'server' | 'hybrid'
    storageMode: "local", // 默认使用本地存储

    // 服务器API配置
    serverConfig: {
        baseURL: "https://your-api-server.com/api",
        timeout: 5000, // 请求超时时间（毫秒）
        retryCount: 3,  // 失败重试次数
        // 角色数据的API路径
        characterDataPath: "/characters" // 角色数据路径，例如：/api/characters
    },

    /**
     * 设置存储模式
     * @param {string} mode - 'local' | 'server' | 'hybrid'
     */
    setStorageMode(mode) {
        if (['local', 'server', 'hybrid'].includes(mode)) {
            this.storageMode = mode;
            cc.log(`[CharacterDataAdapter] 存储模式已切换为: ${mode}`);
        } else {
            cc.warn(`[CharacterDataAdapter] 无效的存储模式: ${mode}`);
        }
    },

    /**
     * 保存角色数据（适配器方法）
     * @param {string} characterName - 角色名称
     * @param {Object} data - 角色数据 { level, exp, baseHp, ... }
     * @returns {Promise<boolean>|boolean} 是否保存成功
     */
    async saveCharacterData(characterName, data) {
        switch (this.storageMode) {
            case "local":
                return this._saveLocal(characterName, data);
            case "server":
                return await this._saveServer(characterName, data);
            case "hybrid":
                // 先保存到本地（快速响应），然后同步到服务器
                this._saveLocal(characterName, data);
                return await this._saveServer(characterName, data);
            default:
                return this._saveLocal(characterName, data);
        }
    },

    /**
     * 加载角色数据（适配器方法）
     * @param {string} characterName - 角色名称
     * @returns {Promise<Object|null>|Object|null} 角色数据或null
     */
    async loadCharacterData(characterName) {
        switch (this.storageMode) {
            case "local":
                return this._loadLocal(characterName);
            case "server":
                return await this._loadServer(characterName);
            case "hybrid":
                // 先从服务器加载，失败则使用本地缓存
                try {
                    const serverData = await this._loadServer(characterName);
                    // 同步到本地缓存
                    this._saveLocal(characterName, serverData);
                    return serverData;
                } catch (e) {
                    cc.warn(`[CharacterDataAdapter] 服务器加载失败，使用本地缓存: ${e.message}`);
                    return this._loadLocal(characterName);
                }
            default:
                return this._loadLocal(characterName);
        }
    },

    /**
     * 本地存储：保存数据
     * @private
     */
    _saveLocal(characterName, data) {
        try {
            const key = "character_data_" + characterName;
            const json = JSON.stringify(data);
            cc.sys.localStorage.setItem(key, json);
            return true;
        } catch (e) {
            cc.error(`[CharacterDataAdapter] 本地保存失败: ${e.message}`);
            return false;
        }
    },

    /**
     * 本地存储：加载数据
     * @private
     */
    _loadLocal(characterName) {
        try {
            const key = "character_data_" + characterName;
            const json = cc.sys.localStorage.getItem(key);
            return json ? JSON.parse(json) : null;
        } catch (e) {
            cc.error(`[CharacterDataAdapter] 本地加载失败: ${e.message}`);
            return null;
        }
    },

    /**
     * 服务器存储：保存数据
     * @private
     */
    async _saveServer(characterName, data) {
        try {
            const url = `${this.serverConfig.baseURL}${this.serverConfig.characterDataPath}/${characterName}`;
            
            const response = await this._httpRequest('PUT', url, data);

            if (response.success) {
                cc.log(`[CharacterDataAdapter] 服务器保存成功: ${characterName}`);
                return true;
            } else {
                throw new Error(response.message || "服务器保存失败");
            }
        } catch (e) {
            cc.error(`[CharacterDataAdapter] 服务器保存失败: ${e.message}`);
            throw e;
        }
    },

    /**
     * 服务器存储：加载数据
     * @private
     */
    async _loadServer(characterName) {
        try {
            const url = `${this.serverConfig.baseURL}${this.serverConfig.characterDataPath}/${characterName}`;
            
            const response = await this._httpRequest('GET', url);

            if (response.success && response.data) {
                cc.log(`[CharacterDataAdapter] 服务器加载成功: ${characterName}`);
                return response.data;
            } else {
                throw new Error(response.message || "服务器加载失败");
            }
        } catch (e) {
            cc.error(`[CharacterDataAdapter] 服务器加载失败: ${e.message}`);
            throw e;
        }
    },

    /**
     * HTTP请求封装（支持重试）
     * @private
     * @param {string} method - HTTP方法
     * @param {string} url - 请求URL
     * @param {Object} data - 请求数据
     * @returns {Promise<Object>} 响应数据
     */
    _httpRequest(method, url, data = null) {
        return new Promise((resolve, reject) => {
            let retryCount = 0;

            const doRequest = () => {
                const xhr = new XMLHttpRequest();
                xhr.open(method, url, true);
                xhr.setRequestHeader('Content-Type', 'application/json');
                
                // 添加身份验证头（如果需要）
                if (this.serverConfig.headers) {
                    Object.keys(this.serverConfig.headers).forEach(key => {
                        xhr.setRequestHeader(key, this.serverConfig.headers[key]);
                    });
                }

                // 设置超时
                xhr.timeout = this.serverConfig.timeout;

                xhr.onload = () => {
                    if (xhr.status >= 200 && xhr.status < 300) {
                        try {
                            const response = JSON.parse(xhr.responseText);
                            resolve(response);
                        } catch (e) {
                            resolve({ success: true, data: xhr.responseText });
                        }
                    } else {
                        if (retryCount < this.serverConfig.retryCount) {
                            retryCount++;
                            cc.log(`[CharacterDataAdapter] 请求失败，重试 ${retryCount}/${this.serverConfig.retryCount}`);
                            setTimeout(doRequest, 1000 * retryCount); // 递增延迟
                        } else {
                            reject(new Error(`HTTP ${xhr.status}: ${xhr.statusText}`));
                        }
                    }
                };

                xhr.onerror = () => {
                    if (retryCount < this.serverConfig.retryCount) {
                        retryCount++;
                        cc.log(`[CharacterDataAdapter] 网络错误，重试 ${retryCount}/${this.serverConfig.retryCount}`);
                        setTimeout(doRequest, 1000 * retryCount);
                    } else {
                        reject(new Error("网络请求失败"));
                    }
                };

                xhr.ontimeout = () => {
                    if (retryCount < this.serverConfig.retryCount) {
                        retryCount++;
                        cc.log(`[CharacterDataAdapter] 请求超时，重试 ${retryCount}/${this.serverConfig.retryCount}`);
                        setTimeout(doRequest, 1000 * retryCount);
                    } else {
                        reject(new Error("请求超时"));
                    }
                };

                if (data) {
                    xhr.send(JSON.stringify(data));
                } else {
                    xhr.send();
                }
            };

            doRequest();
        });
    }
};

module.exports = CharacterDataAdapter;
```

### 2.2 修改 CharacterDataManager 使用适配器

修改 `CharacterDataManager.js` 以使用适配器：

```javascript
/**
 * 角色数据管理器
 * 负责保存和加载角色的等级、经验值等数据
 * 
 * 注意：现在使用 CharacterDataAdapter 作为数据层，支持本地存储和服务器存储的切换
 * 要切换到服务器模式，只需调用：CharacterDataAdapter.setStorageMode('server')
 */
const CharacterDataAdapter = require("CharacterDataAdapter");

var CharacterDataManager = {
    // 存储键前缀（仅用于本地存储）
    STORAGE_PREFIX: "character_data_",

    /**
     * 保存角色数据
     * @param {string} characterName - 角色名称（唯一标识）
     * @param {Object} data - 角色数据 { level, exp, ... }
     * @returns {Promise<boolean>|boolean} 是否保存成功（服务器模式下返回Promise）
     */
    saveCharacterData(characterName, data) {
        if (!characterName) {
            cc.warn("[CharacterDataManager] 角色名称为空，无法保存数据");
            return false;
        }

        const result = CharacterDataAdapter.saveCharacterData(characterName, data);

        // 如果是Promise（服务器模式），返回Promise
        if (result instanceof Promise) {
            return result.then(success => {
                if (success) {
                    cc.log(`[CharacterDataManager] 保存角色数据: ${characterName}`, data);
                }
                return success;
            });
        }

        // 本地模式，直接返回结果
        if (result) {
            cc.log(`[CharacterDataManager] 保存角色数据: ${characterName}`, data);
        }
        return result;
    },

    /**
     * 加载角色数据
     * @param {string} characterName - 角色名称
     * @returns {Promise<Object|null>|Object|null} 角色数据 { level, exp, ... } 或 null（服务器模式下返回Promise）
     */
    loadCharacterData(characterName) {
        if (!characterName) {
            return null;
        }

        const result = CharacterDataAdapter.loadCharacterData(characterName);

        // 如果是Promise（服务器模式），返回Promise
        if (result instanceof Promise) {
            return result.then(data => {
                if (data) {
                    cc.log(`[CharacterDataManager] 加载角色数据: ${characterName}`, data);
                }
                return data;
            });
        }

        // 本地模式，直接返回结果
        if (result) {
            cc.log(`[CharacterDataManager] 加载角色数据: ${characterName}`, result);
        }
        return result;
    },

    /**
     * 保存角色的等级和经验值
     * @param {cc.Node} characterNode - 角色节点
     * @returns {Promise<boolean>|boolean} 是否保存成功（服务器模式下返回Promise）
     */
    saveCharacterLevel(characterNode) {
        const StatsComponent = require("StatsComponent");
        const stats = characterNode.getComponent(StatsComponent);

        if (!stats) {
            cc.warn(`[CharacterDataManager] 节点 ${characterNode.name} 没有 StatsComponent 组件`);
            return false;
        }

        // 获取角色名称（优先使用原始名称）
        let characterName = characterNode.name;
        if (characterNode._originalCharacterName) {
            characterName = characterNode._originalCharacterName;
        } else if (characterName.startsWith("Display_")) {
            characterName = characterName.replace("Display_", "");
        }

        const data = {
            level: stats.level,
            exp: stats.exp,
            baseHp: stats.baseHp,
            baseAttack: stats.baseAttack,
            baseDefense: stats.baseDefense,
            baseSpeed: stats.baseSpeed,
            baseCrit: stats.baseCrit,
            baseMiss: stats.baseMiss,
            saveTime: Date.now()
        };

        return this.saveCharacterData(characterName, data);
    },

    /**
     * 加载角色的等级和经验值
     * @param {string} characterName - 角色名称
     * @returns {Promise<Object|null>|Object|null} { level, exp, ... } 或 null（服务器模式下返回Promise）
     */
    loadCharacterLevel(characterName) {
        return this.loadCharacterData(characterName);
    },

    /**
     * 删除角色数据
     * @param {string} characterName - 角色名称
     * @returns {Promise<boolean>|boolean} 是否删除成功（服务器模式下返回Promise）
     */
    deleteCharacterData(characterName) {
        if (!characterName) {
            return false;
        }

        // 注意：删除操作在服务器模式下需要实现
        // 这里先实现本地删除
        try {
            const key = this.STORAGE_PREFIX + characterName;
            cc.sys.localStorage.removeItem(key);
            cc.log(`[CharacterDataManager] 删除角色数据: ${characterName}`);
            return true;
        } catch (e) {
            cc.error(`[CharacterDataManager] 删除失败: ${e.message}`);
            return false;
        }
    },

    /**
     * 获取所有保存的角色数据
     * @returns {Promise<Object>|Object} { characterName: data, ... }（服务器模式下返回Promise）
     */
    getAllCharacterData() {
        // 注意：在服务器模式下，需要调用服务器API获取所有角色数据
        // 这里先实现本地版本
        const result = {};
        try {
            const keys = Object.keys(cc.sys.localStorage);
            keys.forEach(key => {
                if (key.startsWith(this.STORAGE_PREFIX)) {
                    const characterName = key.replace(this.STORAGE_PREFIX, "");
                    const data = this.loadCharacterData(characterName);
                    if (data) {
                        result[characterName] = data;
                    }
                }
            });
        } catch (e) {
            cc.error(`[CharacterDataManager] 获取所有数据失败: ${e.message}`);
        }
        return result;
    },

    /**
     * 清除所有角色数据
     * @returns {Promise<boolean>|boolean} 是否清除成功（服务器模式下返回Promise）
     */
    clearAllCharacterData() {
        // 注意：在服务器模式下，需要调用服务器API清除所有数据
        // 这里先实现本地版本
        try {
            const keys = Object.keys(cc.sys.localStorage);
            keys.forEach(key => {
                if (key.startsWith(this.STORAGE_PREFIX)) {
                    cc.sys.localStorage.removeItem(key);
                }
            });
            cc.log("[CharacterDataManager] 已清除所有角色数据");
            return true;
        } catch (e) {
            cc.error(`[CharacterDataManager] 清除失败: ${e.message}`);
            return false;
        }
    },

    /**
     * 重置角色的等级和经验值（重置为1级，0经验）
     * @param {string} characterName - 角色名称
     * @returns {Promise<boolean>|boolean} 是否重置成功（服务器模式下返回Promise）
     */
    resetCharacterLevel(characterName) {
        if (!characterName) {
            return false;
        }

        // 删除保存的数据（下次加载时会使用默认值1级，0经验）
        return this.deleteCharacterData(characterName);
    }
};

module.exports = CharacterDataManager;
```

### 2.3 等级系统服务器API规范

#### 获取角色数据

**请求：**
```
GET /api/characters/{characterName}
Headers:
  Authorization: Bearer {token}  // 可选
```

**响应：**
```json
{
  "success": true,
  "data": {
    "level": 6,
    "exp": 1200,
    "baseHp": 120,
    "baseAttack": 18,
    "baseDefense": 1,
    "baseSpeed": 2,
    "baseCrit": 0,
    "baseMiss": 0,
    "saveTime": 1234567890
  }
}
```

#### 保存角色数据

**请求：**
```
PUT /api/characters/{characterName}
Content-Type: application/json
Headers:
  Authorization: Bearer {token}  // 可选

Body:
{
  "level": 6,
  "exp": 1200,
  "baseHp": 120,
  "baseAttack": 18,
  "baseDefense": 1,
  "baseSpeed": 2,
  "baseCrit": 0,
  "baseMiss": 0,
  "saveTime": 1234567890
}
```

**响应：**
```json
{
  "success": true,
  "message": "保存成功"
}
```

---

## 第三部分：统一配置和初始化

### 3.1 创建服务器配置管理器

创建 `ServerConfig.js` 统一管理服务器配置：

```javascript
/**
 * 服务器配置管理器
 * 统一管理所有系统的服务器配置
 */
var ServerConfig = {
    // 服务器基础URL
    baseURL: "https://your-api-server.com/api",
    
    // 请求超时时间（毫秒）
    timeout: 5000,
    
    // 失败重试次数
    retryCount: 3,
    
    // 身份验证配置
    auth: {
        enabled: false, // 是否启用身份验证
        token: null,    // 用户token
        headerName: "Authorization", // 请求头名称
        headerValue: null // 请求头值，例如："Bearer {token}"
    },
    
    /**
     * 初始化服务器配置
     * @param {Object} config - 配置对象
     */
    init(config) {
        if (config.baseURL) this.baseURL = config.baseURL;
        if (config.timeout) this.timeout = config.timeout;
        if (config.retryCount) this.retryCount = config.retryCount;
        if (config.auth) {
            this.auth = { ...this.auth, ...config.auth };
        }
        
        // 更新所有适配器的配置
        this._updateAdapters();
    },
    
    /**
     * 更新所有适配器的配置
     * @private
     */
    _updateAdapters() {
        // 更新道具系统适配器
        const ItemDataAdapter = require("ItemDataAdapter");
        ItemDataAdapter.serverConfig.baseURL = this.baseURL;
        ItemDataAdapter.serverConfig.timeout = this.timeout;
        ItemDataAdapter.serverConfig.retryCount = this.retryCount;
        
        // 更新等级系统适配器
        const CharacterDataAdapter = require("CharacterDataAdapter");
        CharacterDataAdapter.serverConfig.baseURL = this.baseURL;
        CharacterDataAdapter.serverConfig.timeout = this.timeout;
        CharacterDataAdapter.serverConfig.retryCount = this.retryCount;
        
        // 设置身份验证头
        if (this.auth.enabled && this.auth.headerValue) {
            ItemDataAdapter.serverConfig.headers = {
                [this.auth.headerName]: this.auth.headerValue
            };
            CharacterDataAdapter.serverConfig.headers = {
                [this.auth.headerName]: this.auth.headerValue
            };
        }
    },
    
    /**
     * 设置用户token
     * @param {string} token - 用户token
     */
    setAuthToken(token) {
        this.auth.token = token;
        this.auth.headerValue = `Bearer ${token}`;
        this._updateAdapters();
    }
};

module.exports = ServerConfig;
```

### 3.2 在游戏启动时初始化

在主菜单或游戏启动脚本中初始化：

```javascript
// MainMenuScene.js 或 main.js
const ServerConfig = require("ServerConfig");
const ItemDataAdapter = require("ItemDataAdapter");
const CharacterDataAdapter = require("CharacterDataAdapter");

// 初始化服务器配置
ServerConfig.init({
    baseURL: "https://your-api-server.com/api",
    timeout: 5000,
    retryCount: 3,
    auth: {
        enabled: true,
        token: "user_token_here" // 从登录系统获取
    }
});

// 切换到混合模式（推荐）
ItemDataAdapter.setStorageMode("hybrid");
CharacterDataAdapter.setStorageMode("hybrid");
```

---

## 第四部分：代码修改清单

### 4.1 需要创建的文件

1. ✅ `CharacterDataAdapter.js` - 角色数据适配器（新建）
2. ✅ `ServerConfig.js` - 服务器配置管理器（新建）

### 4.2 需要修改的文件

1. ✅ `CharacterDataManager.js` - 使用 `CharacterDataAdapter` 替代直接使用 localStorage
2. ✅ `ItemSystem.js` - 确保使用 `CharacterDataManager` 的方法（已支持）
3. ✅ `LevelSystem.js` - 确保使用 `CharacterDataManager` 的方法（已支持）

### 4.3 需要更新的调用代码

所有调用 `CharacterDataManager` 的地方需要支持异步：

```javascript
// 修改前（同步）
const data = CharacterDataManager.loadCharacterLevel(characterName);

// 修改后（支持异步）
const data = await CharacterDataManager.loadCharacterLevel(characterName);
```

主要需要修改的文件：
- `BattleController.js` - `initEntity` 方法
- `CharacterViewUI.js` - `_initCharacterStats` 方法
- `SelectSceneUI.js` - `_initCharacterStats` 方法

---

## 第五部分：服务器端实现

### 5.1 数据库设计

#### 角色数据表（characters）

```sql
CREATE TABLE characters (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,              -- 用户ID
    character_name VARCHAR(50) NOT NULL, -- 角色名称
    level INT DEFAULT 1,               -- 等级
    exp INT DEFAULT 0,                 -- 经验值
    base_hp INT DEFAULT 100,           -- 基础生命值
    base_attack INT DEFAULT 1,         -- 基础攻击力
    base_defense INT DEFAULT 1,        -- 基础防御力
    base_speed INT DEFAULT 1,          -- 基础速度
    base_crit DECIMAL(5,2) DEFAULT 0, -- 基础暴击率
    base_miss DECIMAL(5,2) DEFAULT 0, -- 基础闪避率
    save_time BIGINT,                  -- 保存时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_character (user_id, character_name)
);
```

#### 道具数据表（inventory）

```sql
CREATE TABLE inventory (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,              -- 用户ID
    item_id VARCHAR(50) NOT NULL,      -- 道具ID
    count INT DEFAULT 0,               -- 道具数量
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_item (user_id, item_id)
);
```

### 5.2 API接口实现示例（Node.js/Express）

#### 角色数据API

```javascript
// GET /api/characters/:characterName
app.get('/api/characters/:characterName', authenticateToken, async (req, res) => {
    try {
        const { characterName } = req.params;
        const userId = req.user.id; // 从token中获取用户ID
        
        const character = await db.query(
            'SELECT * FROM characters WHERE user_id = ? AND character_name = ?',
            [userId, characterName]
        );
        
        if (character.length === 0) {
            return res.json({
                success: true,
                data: null // 没有数据，返回null
            });
        }
        
        const data = character[0];
        res.json({
            success: true,
            data: {
                level: data.level,
                exp: data.exp,
                baseHp: data.base_hp,
                baseAttack: data.base_attack,
                baseDefense: data.base_defense,
                baseSpeed: data.base_speed,
                baseCrit: data.base_crit,
                baseMiss: data.base_miss,
                saveTime: data.save_time
            }
        });
    } catch (error) {
        res.status(500).json({
            success: false,
            message: error.message
        });
    }
});

// PUT /api/characters/:characterName
app.put('/api/characters/:characterName', authenticateToken, async (req, res) => {
    try {
        const { characterName } = req.params;
        const userId = req.user.id;
        const { level, exp, baseHp, baseAttack, baseDefense, baseSpeed, baseCrit, baseMiss, saveTime } = req.body;
        
        await db.query(
            `INSERT INTO characters (user_id, character_name, level, exp, base_hp, base_attack, base_defense, base_speed, base_crit, base_miss, save_time)
             VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
             ON DUPLICATE KEY UPDATE
             level = VALUES(level),
             exp = VALUES(exp),
             base_hp = VALUES(base_hp),
             base_attack = VALUES(base_attack),
             base_defense = VALUES(base_defense),
             base_speed = VALUES(base_speed),
             base_crit = VALUES(base_crit),
             base_miss = VALUES(base_miss),
             save_time = VALUES(save_time)`,
            [userId, characterName, level, exp, baseHp, baseAttack, baseDefense, baseSpeed, baseCrit, baseMiss, saveTime]
        );
        
        res.json({
            success: true,
            message: "保存成功"
        });
    } catch (error) {
        res.status(500).json({
            success: false,
            message: error.message
        });
    }
});
```

#### 道具数据API

```javascript
// GET /api/inventory
app.get('/api/inventory', authenticateToken, async (req, res) => {
    try {
        const userId = req.user.id;
        
        const items = await db.query(
            'SELECT item_id as itemId, count FROM inventory WHERE user_id = ?',
            [userId]
        );
        
        res.json({
            success: true,
            data: {
                items: items
            }
        });
    } catch (error) {
        res.status(500).json({
            success: false,
            message: error.message
        });
    }
});

// PUT /api/inventory
app.put('/api/inventory', authenticateToken, async (req, res) => {
    try {
        const userId = req.user.id;
        const { items } = req.body;
        
        // 使用事务确保数据一致性
        await db.beginTransaction();
        
        try {
            // 删除所有现有道具
            await db.query('DELETE FROM inventory WHERE user_id = ?', [userId]);
            
            // 插入新道具
            if (items && items.length > 0) {
                const values = items.map(item => [userId, item.itemId, item.count]);
                await db.query(
                    'INSERT INTO inventory (user_id, item_id, count) VALUES ?',
                    [values]
                );
            }
            
            await db.commit();
            
            res.json({
                success: true,
                message: "保存成功"
            });
        } catch (error) {
            await db.rollback();
            throw error;
        }
    } catch (error) {
        res.status(500).json({
            success: false,
            message: error.message
        });
    }
});
```

---

## 第六部分：迁移步骤

### 阶段1：准备阶段（开发环境）

1. **创建适配器文件**
   - 创建 `CharacterDataAdapter.js`
   - 创建 `ServerConfig.js`

2. **修改 CharacterDataManager**
   - 使用 `CharacterDataAdapter` 替代直接使用 localStorage
   - 确保所有方法支持异步

3. **更新调用代码**
   - 将所有 `CharacterDataManager` 的调用改为异步（使用 `await`）

4. **测试本地模式**
   - 确保修改后本地模式仍然正常工作

### 阶段2：服务器开发阶段

1. **创建数据库表**
   - 创建 `characters` 表
   - 创建 `inventory` 表

2. **实现API接口**
   - 实现角色数据API（GET/PUT）
   - 实现道具数据API（GET/PUT）
   - 添加身份验证中间件

3. **测试API**
   - 使用 Postman 或 curl 测试API
   - 确保返回格式正确

### 阶段3：集成测试阶段

1. **配置服务器地址**
   ```javascript
   ServerConfig.init({
       baseURL: "https://test-api-server.com/api",
       auth: {
           enabled: true,
           token: "test_token"
       }
   });
   ```

2. **使用混合模式测试**
   ```javascript
   ItemDataAdapter.setStorageMode("hybrid");
   CharacterDataAdapter.setStorageMode("hybrid");
   ```

3. **验证数据同步**
   - 在游戏中修改数据
   - 检查服务器数据库是否正确保存
   - 重新加载游戏，检查数据是否正确加载

### 阶段4：正式切换

1. **切换到服务器模式**
   ```javascript
   ItemDataAdapter.setStorageMode("server");
   CharacterDataAdapter.setStorageMode("server");
   ```

2. **监控错误日志**
   - 检查控制台是否有错误
   - 检查服务器日志

3. **准备回滚方案**
   - 可以随时切回本地模式
   - 保留本地数据作为备份

---

## 第七部分：注意事项

### 7.1 异步处理

所有数据操作在服务器模式下都是异步的，需要使用 `await` 或 `.then()`：

```javascript
// ✅ 正确
const data = await CharacterDataManager.loadCharacterLevel(characterName);

// ❌ 错误（服务器模式下会返回Promise）
const data = CharacterDataManager.loadCharacterLevel(characterName);
```

### 7.2 错误处理

服务器模式下需要处理网络错误：

```javascript
try {
    await CharacterDataManager.saveCharacterData(characterName, data);
} catch (error) {
    cc.error(`保存失败: ${error.message}`);
    // 可以显示错误提示给用户
}
```

### 7.3 数据一致性

- 使用混合模式时，本地缓存和服务器数据可能不一致
- 建议在关键操作后强制同步到服务器
- 定期从服务器拉取最新数据

### 7.4 性能优化

1. **批量操作**：如果可能，实现批量保存接口
2. **本地缓存**：使用混合模式减少服务器请求
3. **请求去重**：避免同一时间发送重复请求

### 7.5 安全建议

1. **身份验证**：所有API都应该验证用户身份
2. **数据验证**：服务器端验证数据格式和合法性
3. **防作弊**：服务器端验证升级逻辑，防止客户端作弊
4. **HTTPS**：生产环境必须使用HTTPS

---

## 第八部分：完整示例

### 8.1 游戏启动配置

```javascript
// main.js 或 MainMenuScene.js
const ServerConfig = require("ServerConfig");
const ItemDataAdapter = require("ItemDataAdapter");
const CharacterDataAdapter = require("CharacterDataAdapter");

// 检查是否已登录
const userToken = getUserToken(); // 从登录系统获取

if (userToken) {
    // 初始化服务器配置
    ServerConfig.init({
        baseURL: "https://api.yourgame.com/api",
        timeout: 5000,
        retryCount: 3,
        auth: {
            enabled: true,
            token: userToken
        }
    });
    
    // 切换到混合模式（推荐）
    ItemDataAdapter.setStorageMode("hybrid");
    CharacterDataAdapter.setStorageMode("hybrid");
    
    cc.log("[Game] 已切换到服务器模式（混合模式）");
} else {
    // 未登录，使用本地模式
    ItemDataAdapter.setStorageMode("local");
    CharacterDataAdapter.setStorageMode("local");
    
    cc.log("[Game] 使用本地存储模式");
}
```

### 8.2 修改 BattleController 支持异步

```javascript
// BattleController.js - initEntity 方法
async initEntity(node, data, teamName) {
    // ... 其他代码 ...
    
    // 加载保存的数据（支持异步）
    const CharacterDataManager = require("CharacterDataManager");
    const savedData = await CharacterDataManager.loadCharacterLevel(data.name || node.name);
    
    // ... 后续代码 ...
}
```

---

## 第九部分：快速开始

### 9.1 完整接入步骤（按顺序执行）

#### 步骤1：创建适配器文件（已完成）

✅ 已创建 `CharacterDataAdapter.js`
✅ 已创建 `ServerConfig.js`
✅ 已修改 `CharacterDataManager.js` 使用适配器

#### 步骤2：更新调用代码（已完成）

✅ 已修改 `BattleController.js` 的 `initEntity` 和 `_initAllUnits` 方法支持异步
✅ 已修改 `CharacterViewUI.js` 的 `_initCharacterStats` 方法支持异步
✅ 已修改 `SelectSceneUI.js` 的 `_initCharacterStats` 方法支持异步

#### 步骤3：配置服务器

在游戏启动时（如 `MainMenuScene.js` 或 `main.js`）添加：

```javascript
const ServerConfig = require("ServerConfig");
const ItemDataAdapter = require("ItemDataAdapter");
const CharacterDataAdapter = require("CharacterDataAdapter");

// 初始化服务器配置
ServerConfig.init({
    baseURL: "https://your-api-server.com/api",
    timeout: 5000,
    retryCount: 3,
    auth: {
        enabled: true,
        token: getUserToken() // 从登录系统获取
    }
});

// 切换到混合模式（推荐用于测试）
ItemDataAdapter.setStorageMode("hybrid");
CharacterDataAdapter.setStorageMode("hybrid");
```

#### 步骤4：实现服务器API

按照本文档第五部分的API规范实现服务器接口。

#### 步骤5：测试

1. 运行游戏
2. 检查控制台日志，确认数据同步
3. 验证数据是否正确保存到服务器
4. 重新加载游戏，验证数据是否正确加载

#### 步骤6：切换到生产模式

测试通过后，切换到服务器模式：

```javascript
ItemDataAdapter.setStorageMode("server");
CharacterDataAdapter.setStorageMode("server");
```

---

## 第十部分：常见问题

### Q1: 数据加载失败怎么办？

**A:** 检查以下几点：
1. 服务器API地址是否正确
2. 网络连接是否正常
3. 身份验证token是否有效
4. 服务器返回格式是否符合规范

### Q2: 混合模式下数据不一致？

**A:** 混合模式下，本地缓存和服务器数据可能不一致。建议：
1. 在关键操作后强制同步到服务器
2. 定期从服务器拉取最新数据
3. 游戏启动时优先从服务器加载

### Q3: 如何调试服务器请求？

**A:** 在浏览器开发者工具的Network标签中查看：
1. 请求URL是否正确
2. 请求头是否包含身份验证信息
3. 响应状态码和内容

### Q4: 性能问题？

**A:** 优化建议：
1. 使用混合模式减少服务器请求
2. 实现批量操作接口
3. 添加请求去重机制
4. 使用本地缓存

---

## 总结

### 接入优势

1. ✅ **架构设计良好**：使用适配器模式，数据层和业务层分离
2. ✅ **代码改动小**：只需要修改数据管理器，业务逻辑无需改动
3. ✅ **渐进式迁移**：可以先使用混合模式，逐步切换
4. ✅ **向后兼容**：可以随时切回本地模式

### 已完成的工作

1. ✅ 创建了 `CharacterDataAdapter.js` - 角色数据适配器
2. ✅ 创建了 `ServerConfig.js` - 服务器配置管理器
3. ✅ 修改了 `CharacterDataManager.js` - 使用适配器替代直接使用localStorage
4. ✅ 更新了所有调用代码 - 支持异步操作

### 接下来需要做的

1. 实现服务器API接口（按照第五部分的API规范）
2. 配置服务器地址和身份验证
3. 测试数据同步
4. 切换到生产模式

### 需要帮助？

如果在接入过程中遇到问题，可以：
1. 检查控制台日志
2. 检查服务器API返回格式
3. 使用混合模式进行调试
4. 查看本文档的常见问题部分
