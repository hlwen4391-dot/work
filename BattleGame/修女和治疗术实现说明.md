# 💚 修女英雄和治疗术实现说明

## 📋 实现内容

已成功添加：
1. ✅ **修女英雄** - 新的治疗型英雄
2. ✅ **群体治疗术** - 恢复己方阵营所有英雄的生命值

---

## 🔧 代码修改详情

### 1. SkillConfig.js - 添加治疗技能枚举和配置

**修改位置**: `system/SkillConfig.js`

#### 添加技能枚举
```javascript
var SkillEnum = {
    // ... 其他技能 ...
    healAllies: 9,    // 群体治疗
    // ...
};
```

#### 添加治疗技能配置
```javascript
// 群体治疗 - 恢复己方阵营所有英雄的生命值
healAllies: {
    name: "治疗术",
    id: SkillEnum.healAllies,
    cooldown: 5.0,
    requireRage: 0,  // 普通技能，不需要怒气值
    effect: (self, target, log) => {
        const teamComp = self.getComponent("TeamComponent");
        if (!teamComp) return [];

        const allies = teamComp.team === "hero"
            ? TeamRef.herosRef
            : TeamRef.monstersRef;

        log(`💚 ${self.name} 释放群体治疗术！`);

        // 为所有队友恢复生命值
        const healAmount = 30;  // 基础治疗量
        const events = [];
        
        for (let ally of allies) {
            const allyStats = ally.getComponent("StatsComponent");
            if (allyStats && !allyStats.isDead()) {
                // 添加治疗事件
                events.push({
                    type: "heal",
                    target: ally,
                    value: healAmount
                });
            }
        }

        return events;
    }
}
```

**关键特性**:
- 治疗量：30点HP
- 冷却时间：5秒
- 目标：己方阵营所有活着的单位
- 不需要怒气值

---

### 2. CombatSystem.js - 添加治疗逻辑

**修改位置**: `system/CombatSystem.js`

#### 添加 heal() 方法
```javascript
/**
 * 治疗（恢复生命值）
 * @param {cc.Node} caster - 施法者
 * @param {cc.Node} target - 目标
 * @param {number} healAmount - 治疗量
 * @param {Function} log - 日志函数
 * @param {Object} recorder - 战斗记录器（可选）
 */
heal(caster, target, healAmount, log, recorder) {
    const tgtStats = target.getComponent("StatsComponent");
    if (!tgtStats) return;

    // 如果目标已死亡，无法治疗
    if (tgtStats.isDead()) {
        log(`💚 ${target.name} 已死亡，无法治疗`);
        return;
    }

    // 计算实际恢复量（不能超过最大HP）
    const actualHeal = Math.min(healAmount, tgtStats.maxHp - tgtStats.hp);
    
    if (actualHeal > 0) {
        // 恢复HP
        tgtStats.hp += actualHeal;
        tgtStats.hp = Math.min(tgtStats.hp, tgtStats.maxHp);  // 确保不超过最大HP
        
        // 更新血条显示（使用'heal'类型）
        tgtStats.updateHealthBar(actualHeal, 'heal');
        
        log(`💚 ${caster.name} 对 ${target.name} 恢复了 ${actualHeal} 点生命值 (当前HP: ${tgtStats.hp}/${tgtStats.maxHp})`);
        
        // 记录治疗事件
        if (recorder) {
            recorder.recordHeal(caster, target, actualHeal);
        }
    } else {
        log(`💚 ${target.name} 生命值已满，无法治疗`);
    }
}
```

**功能说明**:
- 检查目标是否死亡（已死亡无法治疗）
- 计算实际恢复量（不超过最大HP）
- 更新血条显示（绿色治疗数字）
- 记录治疗事件（用于回放）

---

### 3. SkillSystem.js - 添加治疗事件处理

**修改位置**: `system/SkillSystem.js`

#### 在 useSkill() 中添加 heal 事件处理
```javascript
for (let evt of events) {
    switch (evt.type) {
        // ... 其他事件类型 ...
        case "heal":
            // 治疗事件：evt.target 是目标单位，evt.value 是治疗量
            const healTarget = evt.target || target;
            CombatSystem.heal(entity, healTarget, evt.value, log, recorder);
            break;
        // ...
    }
}
```

#### 更新技能ID到名称的映射
```javascript
const skillIdToName = {
    // ... 其他技能 ...
    9: "治疗术"
};
```

---

### 4. UnitDataConfig.js - 添加修女英雄

**修改位置**: `game/UnitDataConfig.js`

#### 添加修女英雄配置
```javascript
{
    name: "修女",
    displayName: "修女",
    icon: null, // 头像图片资源（SpriteFrame，需要在编辑器中设置）
    prefab: null, // 人物原型Prefab（需要在编辑器中设置）
    avatarPosition: 2, // 头像固定位置索引（2 = 第三个位置）
    hp: 100,
    attack: 5,
    defense: 6,
    speed: 10,
    crit: 0.05,
    miss: 0.05,
    skills: [
        SkillConfig.normalAttack,
        SkillConfig.healAllies
    ]
}
```

**属性说明**:
- **HP**: 100（中等生命值）
- **攻击**: 5（较低，治疗型角色）
- **防御**: 6（中等防御）
- **速度**: 10（中等速度）
- **暴击率**: 5%（较低）
- **闪避率**: 5%（较低）
- **技能**: 普通攻击 + 治疗术

---

### 5. BattleRecorder.js - 添加治疗事件记录

**修改位置**: `system/BattleRecorder.js`

#### 添加 recordHeal() 方法
```javascript
/**
 * 记录治疗事件
 * @param {cc.Node} caster - 施法者
 * @param {cc.Node} target - 目标
 * @param {number} healAmount - 治疗量
 */
recordHeal(caster, target, healAmount) {
    const targetStats = target.getComponent("StatsComponent");
    this.recordEvent("heal", {
        casterName: caster.name,
        casterId: this._getEntityId(caster),
        targetName: target.name,
        targetId: this._getEntityId(target),
        healAmount: healAmount,
        targetHp: targetStats ? targetStats.hp : 0,
        targetMaxHp: targetStats ? targetStats.maxHp : 0
    });
}
```

**功能**: 记录治疗事件，用于战斗回放

---

### 6. BattleReplayer.js - 添加治疗事件回放

**修改位置**: `system/BattleReplayer.js`

#### 在 _executeEvent() 中添加 heal 事件处理
```javascript
switch (type) {
    // ... 其他事件类型 ...
    case "heal":
        this._onHeal(data);
        break;
    // ...
}
```

#### 添加 _onHeal() 方法
```javascript
/**
 * 处理治疗事件
 * @private
 */
_onHeal(data) {
    const caster = this.unitMap[data.casterId];
    const target = this.unitMap[data.targetId];

    // 如果目标已死亡，跳过处理
    if (target && this.deadUnits.has(target)) {
        cc.log(`[BattleReplayer] 跳过已死亡单位 ${data.targetName} 的治疗事件`);
        return;
    }

    if (target && target.isValid) {
        const stats = target.getComponent("StatsComponent");
        if (stats) {
            // 恢复HP到记录时的状态
            stats.hp = data.targetHp;
            stats.maxHp = data.targetMaxHp;

            // 更新血条显示（使用'heal'类型）
            stats.updateHealthBar(data.healAmount, 'heal');

            cc.log(`[BattleReplayer] ${data.casterName} 对 ${data.targetName} 恢复了 ${data.healAmount} 点生命值，当前HP: ${stats.hp}/${stats.maxHp}`);
        }
    }
}
```

**功能**: 回放时恢复治疗效果

---

## 🎮 使用方法

### 1. 在编辑器中选择修女

1. 打开 `SelectScene` 场景
2. 在 `SelectSceneUI` 组件中：
   - 将修女的头像图片（SpriteFrame）拖到 `heroIcons` 数组的第3个位置（索引2）
   - 将修女的Prefab拖到 `heroPrefabs` 数组的第3个位置（索引2）

### 2. 配置修女Prefab

确保修女的Prefab包含以下组件：
- ✅ `StatsComponent` - 属性组件
- ✅ `TeamComponent` - 队伍组件（team = "hero"）
- ✅ `SkillComponent` - 技能组件
- ✅ `CombatComponent` - 战斗组件
- ✅ `sp.Skeleton` - Spine动画组件（可选，用于动画）
- ✅ `HealthBar` - 血条组件（用于显示HP和治疗效果）

### 3. 战斗中使用

- 修女会自动使用治疗术（AI会根据冷却时间自动释放）
- 治疗术会恢复己方所有活着的英雄30点HP
- 治疗效果会显示绿色数字（+30）向上飘
- 如果目标HP已满，不会进行治疗

---

## 📊 治疗术效果

### 治疗量
- **基础治疗量**: 30点HP
- **实际治疗量**: `min(30, maxHp - currentHp)`
  - 如果目标HP为70/100，则恢复30点，变为100/100
  - 如果目标HP为95/100，则恢复5点，变为100/100

### 冷却时间
- **冷却时间**: 5秒
- AI会在冷却时间结束后自动释放

### 目标范围
- **己方阵营所有活着的单位**
- 不包括已死亡的单位
- 不包括自己（如果需要治疗自己，可以修改代码）

---

## 🔍 代码流程

```
修女释放治疗术
    ↓
SkillConfig.healAllies.effect()
    ↓
返回 heal 事件列表（每个队友一个事件）
    ↓
SkillSystem.useSkill()
    ↓
处理每个 heal 事件
    ↓
CombatSystem.heal()
    ↓
恢复HP + 更新血条 + 记录事件
    ↓
BattleRecorder.recordHeal()
    ↓
保存到战斗记录
    ↓
回放时：BattleReplayer._onHeal()
    ↓
恢复HP状态
```

---

## ⚙️ 自定义治疗量

如果需要修改治疗量，编辑 `SkillConfig.js` 中的 `healAllies` 配置：

```javascript
healAllies: {
    // ...
    effect: (self, target, log) => {
        // ...
        const healAmount = 50;  // ⭐ 修改这里的数值
        // ...
    }
}
```

---

## 📝 注意事项

1. **Prefab配置**: 确保修女的Prefab正确配置了所有必需的组件
2. **头像和Prefab**: 需要在编辑器中手动绑定修女的头像和Prefab
3. **治疗范围**: 当前只治疗己方阵营，不包括自己
4. **已死亡单位**: 无法治疗已死亡的单位
5. **HP上限**: 治疗不会超过最大HP

---

## ✅ 完成状态

- ✅ 技能枚举已添加
- ✅ 治疗技能配置已添加
- ✅ 治疗逻辑已实现
- ✅ 修女英雄已添加
- ✅ 事件记录已实现
- ✅ 回放支持已实现

现在可以在游戏中选择修女并使用治疗术了！🎉
